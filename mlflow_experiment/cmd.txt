docker build -t mlflow_experiment_image -f mlflow_experiment/Dockerfile .

docker run -p 8080:8080 -v $(pwd)/mlruns:/app/mlruns mlflow_experiment_image

# une fois le container stoppé et supprimé, on peut accéder de nouveau aux expériences en lançant le serveur de tracking MLflow en local
mlflow server --host 0.0.0.0 --port 8080